# FactoryCalculator

Factory game production-rate calculator — core logic in `FactoryCalculator.Core` (targeting .NET 8) with a WinUI front-end.

Quick start (CLI):
1. dotnet build
2. dotnet test

This repo includes a GitHub Actions CI workflow that builds and runs tests for .NET 8 on push/PR.