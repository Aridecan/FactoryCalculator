<Page
    x:Class="FactoryCalculator.Views.GameSetupPage"
    x:Name="PageRoot"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    mc:Ignorable="d"
    xmlns:local="using:FactoryCalculator.Views">

    <ScrollViewer>
        <StackPanel Padding="20" Spacing="12">
            <TextBlock Text="Game Setup" FontSize="20" FontWeight="SemiBold" />

            <StackPanel Orientation="Horizontal" Spacing="8" VerticalAlignment="Center">
                <TextBlock Text="Config Path:" VerticalAlignment="Center" Width="90" />
                <TextBox x:Name="PathTextBox" Width="520" Text="{Binding ConfigPath, Mode=TwoWay}" />
            </StackPanel>

            <StackPanel Orientation="Horizontal" Spacing="8">
                <Button Content="Load" Width="120" Command="{Binding LoadCommand}" />
                <Button Content="Save" Width="120" Command="{Binding SaveCommand}" />
            </StackPanel>

            <StackPanel Orientation="Horizontal" Spacing="8" VerticalAlignment="Center">
                <TextBlock Text="Game Name:" VerticalAlignment="Center" Width="90" />
                <TextBox Width="300" Text="{Binding GameName, Mode=TwoWay}" />
            </StackPanel>

            <StackPanel Orientation="Horizontal" Spacing="8" VerticalAlignment="Center">
                <TextBlock Text="Unit Rate:" VerticalAlignment="Center" Width="90" />
                <ComboBox
                    Width="200"
                    ItemsSource="{Binding UnitRateOptions}"
                    DisplayMemberPath="Display"
                    SelectedValuePath="Value"
                    SelectedValue="{Binding UnitRate, Mode=TwoWay}" />
            </StackPanel>

            <TextBlock Text="Ingredient Types" FontWeight="SemiBold" />
            <StackPanel Orientation="Horizontal" Spacing="8">
                <ComboBox x:Name="IngredientCombo" Width="240" IsEditable="True" ItemsSource="{Binding IngredientTypes}" Text="{Binding NewIngredientType, Mode=TwoWay}" />
                <Button Content="Add" Width="80" Command="{Binding AddIngredientCommand}" />
            </StackPanel>

            <ListView x:Name="IngredientList" ItemsSource="{Binding IngredientTypes}" Height="120">
                <ListView.ItemTemplate>
                    <DataTemplate x:DataType="x:String">
                        <StackPanel Orientation="Horizontal" Spacing="8">
                            <TextBlock Text="{Binding}" VerticalAlignment="Center" Width="220" />
                            <Button Content="Remove" Command="{Binding DataContext.RemoveIngredientCommand, ElementName=PageRoot}" CommandParameter="{Binding}" />
                        </StackPanel>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>

            <TextBlock Text="JSON Preview" FontWeight="SemiBold" />
            <TextBox
                Text="{Binding SerializedJson, Mode=OneWay}"
                AcceptsReturn="True"
                TextWrapping="Wrap"
                ScrollViewer.VerticalScrollBarVisibility="Auto"
                Height="300" />
        </StackPanel>
    </ScrollViewer>
</Page>
